{
  "name": "open-eagle",
  "private": true,
  "version": "0.1.1",
  "main": "dist-electron/main.js",
  "type": "module",
  "packageManager": "pnpm@10.7.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "electron:dev": "node scripts/electron-dev.cjs",
    "backend:sidecar": "node scripts/build-sidecar.cjs",
    "electron:build": "tsc -p tsconfig.electron.json && node scripts/ensure-cjs-package.cjs && vite build && electron-builder --publish never",
    "electron:build:win": "tsc -p tsconfig.electron.json && node scripts/ensure-cjs-package.cjs && vite build && electron-builder --win --publish never",
    "electron:build:mac": "tsc -p tsconfig.electron.json && node scripts/ensure-cjs-package.cjs && vite build && electron-builder --mac --publish never",
    "electron:build:linux": "tsc -p tsconfig.electron.json && node scripts/ensure-cjs-package.cjs && vite build && electron-builder --linux --publish never",
    "package:desktop": "pnpm backend:sidecar && pnpm electron:build",
    "package:windows": "pnpm backend:sidecar && pnpm electron:build:win",
    "package:mac": "pnpm backend:sidecar && pnpm electron:build:mac",
    "package:linux": "pnpm backend:sidecar && pnpm electron:build:linux"
  },
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.6",
    "classic-level": "^3.0.0",
    "lucide-react": "^1.11.0",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^22.19.19",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^42.2.0",
    "electron-builder": "^26.8.1",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.7.3",
    "vite": "^6.2.1"
  }
}
