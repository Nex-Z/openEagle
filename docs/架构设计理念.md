# 架构设计理念

## 1. 混合架构

- 后端使用 Python（FastAPI + WebSocket）承载 Agent 推理、SOLO 编排与业务协议。
- 桌面壳使用 Tauri（Rust）负责进程托管与本地系统能力。
- 前端使用 React + TypeScript 负责对话界面、设置中心、状态可视化。

## 2. 后端部署模型

- 开发环境：由 Tauri 启动本地 Python 环境运行 `app.main`。
- 生产环境：Python 后端可通过 `PyInstaller` 打包为 sidecar 可执行文件。

## 3. 动态端口握手

1. Python 后端以随机端口启动（`port=0`）。
2. 启动成功后输出握手日志：`[AGENT_READY] WS_PORT: <port>`。
3. Rust 监听 sidecar 输出，解析端口并发布给前端。
4. 前端连接 `ws://127.0.0.1:<port>/ws`。

这样可以避免端口冲突，提升多实例与本地环境兼容性。

## 4. 生命周期接管

- Tauri 统一管理 sidecar 启停。
- 主窗口退出时，Rust 主动终止后端子进程，避免孤儿进程。
- 连接断开时前端执行自动重连策略并回显状态。

## 5. SOLO 设计原则

- 模型只做“决策”，执行由本地工具层负责。
- 所有关键步骤通过 `server:solo_status / step / trace` 回传，保证可追踪。
- 危险动作进入显式确认流程，减少误操作风险。
- 自动暂停机制用于防止死循环（如连续重复动作）。
- 稳定性策略保持温和：用 action signature、no-op/uncertain/failed 分类和 recovery mode 提醒模型换路线，但不把 SOLO 改成固定脚本。
- 可修复的坏输出或执行错误先反馈给当前 agent 重新决策，连续失败才暂停给用户判断。

## 6. Main/Sub-Agent 分层

- 所有普通用户消息先进入 main agent。main agent 负责意图理解、路由、汇总，不直接管理 worker 的每一步执行细节。
- sub-agent 使用干净的内部 conversation id 执行任务，完成后只回传摘要、证据和结果，减少主会话上下文污染。
- worker 分为 `general`、`coding`、`research`、`solo`。只读 worker 可保守并发，写入类 worker 与 SOLO 串行。
- SOLO 通过 `SoloWorkerAdapter` 纳入统一调度，但视觉观察、执行、恢复和验证仍由 SOLO 内核自己负责。
- 前端以 `kind="agent"` 的 trace 展示 main-router 和 worker 摘要，用户可展开查看细节，但默认不刷屏。

## 7. 远程 IM 接入

- `IMBridge` 位于后端内部，将飞书、Telegram 的外部消息转换成普通 openEagle 会话事件。
- 每个远程会话会生成稳定的 `im_<channel>_<hash>` 会话 ID，前端仍通过现有 WebSocket Envelope 协议接收消息、SOLO 状态和确认请求。
- 飞书按 `open_id` 或 `chat_id` 白名单放行；Telegram 按 `user_id` 或 `chat_id` 白名单放行。任一命中即可进入会话，白名单为空则拒绝全部远程来源。
- 远程普通文本默认进入 SOLO，减少手机端反复输入命令的成本；`/chat` 用于显式切换到纯文本对话。
- 飞书长连接 SDK 自带 asyncio loop 管理，后端将其隔离在独立线程与专用 loop 中，避免和 FastAPI 主事件循环互相阻塞。

## 8. 显示器与截图策略

- 支持从设置页选择 SOLO 显示器（`preferredDisplayIndex`）。
- 后端按该设置执行截图与坐标映射。
- 对可能触发页面加载的动作做动作后截图稳定采样，避免将过早的 loading 帧作为下一轮模型输入。
- 提供实时显示器预览用于选择校验。
- 为规避 `asset.localhost` 在部分机器对临时目录的访问限制，前端支持通过 Rust 命令读取图片并渲染为 data URL。
